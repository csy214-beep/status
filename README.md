# 状态显示应用

一个简洁的Windows桌面状态显示应用，在屏幕右上角显示当前有效的状态图标。

## 功能特性

- ✅ 屏幕右上角常驻显示
- ✅ 仅显示图标，无文字干扰
- ✅ 鼠标悬停显示详细信息（状态名、注意事项、时间范围）
- ✅ 图标水平排列，每行最多8个
- ✅ 透明背景，简约设计
- ✅ 智能显示当前时间段内的有效状态
- ✅ 自动刷新
- ✅ 支持添加、查看、删除状态
- ✅ JSON格式数据存储
- ✅ 系统托盘集成

## 技术栈

- **语言**: C++17
- **框架**: Qt6
- **构建**: CMake
- **平台**: Windows

## 编译要求

### 必需软件

1. **CMake** (>= 3.16)
    - 下载: https://cmake.org/download/

2. **Qt6** (>= 6.0)
    - 下载: https://www.qt.io/download-qt-installer
    - 安装时选择以下组件:
        - Qt 6.x.x Desktop (MinGW或MSVC)
        - Qt 6.x.x CMake

3. **C++编译器**
    - MinGW (Qt自带) 或
    - Visual Studio 2019/2022 (MSVC)

## 编译步骤

### 使用CLion (推荐)

1. 打开CLion
2. File → Open → 选择 `StatusDisplayApp` 文件夹
3. 配置Qt6路径:
    - File → Settings → Build, Execution, Deployment → CMake
    - 在CMake options中添加: `-DCMAKE_PREFIX_PATH=C:/Qt/6.x.x/msvc2019_64`
      (根据你的Qt安装路径调整)
4. 点击 Build → Build Project
5. 点击 Run 按钮运行

### 使用命令行

```bash
# 创建构建目录
mkdir build
cd build

# 配置CMake (指定Qt路径)
cmake .. -DCMAKE_PREFIX_PATH=C:/Qt/6.x.x/msvc2019_64

# 编译
cmake --build . --config Release

# 运行
./bin/status.exe
```

## 使用说明

### 首次运行

1. 运行程序后，会在右上角显示状态窗口
2. 如果没有状态，会显示"当前未设置状态"
3. 双击系统托盘图标打开管理界面

### 添加状态

1. 打开管理界面（双击托盘图标或右键菜单）
2. 切换到"添加状态"选项卡
3. 输入以下信息:
    - **图标**: 使用emoji表情（Win + . 打开emoji面板）
    - **描述**: 状态说明
    - **起始时间**: 状态开始的小时（0-23）
    - **持续时长**: 状态持续的小时数
4. 点击"添加状态"按钮

### 常用Emoji示例

- 😀 😊 😴 - 心情状态
- 📚 💻 📝 - 工作学习
- ⚡ 🏃 💪 - 运动健身
- 🍔 ☕ 🍕 - 饮食相关
- 🌙 ⏰ 🔔 - 时间提醒

### 查看状态

管理界面提供三个查看选项卡:

- **所有状态**: 显示全部已添加的状态，可删除
- **当前状态**: 显示当前时间段内有效的状态
- **过期状态**: 显示已经过期的状态

### 主窗口操作

- **鼠标左键拖动**: 移动窗口位置
- **鼠标右键**: 显示快捷菜单
- **鼠标悬停图标**: 显示详细信息

### 系统托盘

- **右键菜单**:
    - 管理状态: 打开管理界面
    - 刷新显示: 立即刷新状态
    - 程序文件夹: 打开程序所在文件夹
    - 退出: 关闭应用

## 数据存储

状态数据保存在 `status.json` 文件中，格式如下:

```json
{
  "statuses": [
    {
      "description": "Hello, world!",
      "durationHours": 8,
      "icon": "a",
      "startDateTime": "2026-02-11T15:13:07"
    }
  ]
}

```

- **icon**: emoji图标或文本
- **description**: 状态描述
- **startHour**: 起始小时 (0-23)
- **durationHours**: 持续小时数 (1-24)

## 文件结构

```
StatusDisplayApp/
├── CMakeLists.txt          # CMake构建配置
├── LICENSE
├── assets/                  # 资源文件
├── src/
│   ├── main.cpp            # 程序入口
│   ├── StatusData.h        # 状态数据结构
│   ├── StatusManager.h/.cpp# 状态管理器
│   ├── MainWindow.h/.cpp   # 主显示窗口
│   ├── ManageDialog.h/.cpp # 管理对话框
├── resources/               # Qt资源文件
└── README.md               # 自述文件
```

## 时间计算逻辑

应用支持跨天时间段，例如:

- 起始时间: YYYY-MM-DD 23:00
- 持续时长: 8小时
- 实际时间段: 23:00 - 07:00 (次日)

当前时间在此范围内时，状态会被显示为"当前状态"。

## 自定义设置

可以修改以下常量来自定义显示:

**MainWindow.h**:
```cpp
static const int MAX_ICONS_PER_ROW = 8;  // 每行最多图标数
static const int ICON_SIZE = 48;          // 图标大小(像素)
static const int ICON_SPACING = 10;       // 图标间距(像素)
static const int DEFAULT_SHOW_COUNT = 3;  // 默认显示数量
```

**MainWindow.cpp**:
```cpp
m_refreshTimer->start(3000);  // 刷新间隔(毫秒)
```

```bash
windeployqt status.exe
```

## 许可证

本项目仅供学习和个人使用。

[CC BY-NC-SA 4.0](https://creativecommons.org/licenses/by-nc-sa/4.0)

